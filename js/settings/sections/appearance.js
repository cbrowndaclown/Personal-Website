/* Appearance section — placeholder for future visual settings. */

/**
 * @param {HTMLElement} _body
 * @param {object} _api
 * @returns {{ sync: () => void }}
 */
export function buildAppearanceSection(_body, _api) {
  /* Intentionally empty — ready for toggles, sliders, color pickers later. */
  return { sync() {} };
}
