/* Layout section — placeholder for future layout settings. */

/**
 * @param {HTMLElement} _body
 * @param {object} _api
 * @returns {{ sync: () => void }}
 */
export function buildLayoutSection(_body, _api) {
  /* Intentionally empty — ready for density, sizing, positioning later. */
  return { sync() {} };
}
