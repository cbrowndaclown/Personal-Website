/* Advanced section — placeholder for future advanced settings. */

/**
 * @param {HTMLElement} _body
 * @param {object} _api
 * @returns {{ sync: () => void }}
 */
export function buildAdvancedSection(_body, _api) {
  /* Intentionally empty — ready for debug / experimental controls later. */
  return { sync() {} };
}
